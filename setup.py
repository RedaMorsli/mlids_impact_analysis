from setuptools import setup, find_packages

setup(
    name='mlids_resources',
    version='0.1',
    packages=find_packages(),  # Automatically find all packages and sub-packages
    install_requires=[
        # List of dependencies
    ],
    entry_points={
        'console_scripts': [
            # Define entry points if needed
        ],
    },
)
